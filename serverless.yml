service: ali-ahm-aws-challenge

provider:
  name: aws
  runtime: go1.x
  lambdaHashingVersion: 20201221
  stage: dev
  region: us-east-1
  environment:
    AWS_ACCESS_KEY_ID: ${secrets.AWS_ACCESS_KEY_ID}
    AWS_SECRET_ACCESS_KEY: ${secrets.AWS_SECRET_ACCESS_KEY}
    AWS_TABLE_NAME: ${secrets.AWS_ACCESS_KEY_ID}

functions:
  app:
    handler: bin/main
    events:
      - http:
          path: /{proxy+}
          method: any
          cors: true

resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${secrets.AWS_ACCESS_KEY_ID}
        AttributeDefinitions:
          - AttributeName: id
            AttributeType: S
        KeySchema:
          - AttributeName: id
            KeyType: HASH